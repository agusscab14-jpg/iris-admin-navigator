version: '3.6'
services:
  iris:
    build:
      context: .
    command: --check-caps false
    ports:
      - 52773:52773
    volumes:
      - ./web:/opt/iris-management/csp
      - ./tests:/opt/iris-management/tests
      - ./:/irisdev/app
